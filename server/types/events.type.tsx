export type Event = {
  event_auto_id: number;
  name: string;
  description: string;
  date: string; // or Date for Date objects
  link: string;
  image: string;
  status: number;
  created_at: string; // or Date for Date objects
};
